# PHASE5_LIVE_CONNECT — საჯარო საიტის დაკავშირება Supabase-სთან (ცარიელი, რეალური კატალოგით)

**სტატუსი:** დასამტკიცებელი → შესასრულებელი
**თარიღი:** 2026-09-24

## ფონი / რატომ ვაკეთებთ ამას ახლა

`script.js`-ში ამჟამად ჩაწერილი `DATA` (25 "პროდუქტი") და `HERO_SLIDES` (3 ფერადი გრადიენტი) **სრულად ხელოვნურია** — AI-ის მიერ თავიდან შექმნილი placeholder კონტენტი, არა Doll's Coffee-ს რეალური პროდუქტები. მფლობელს არასდროს მოუწოდებია ეს მონაცემები.

**გადაწყვეტილება:** ეს ყალბი კონტენტი მთლიანად ამოღებულია კოდიდან. საიტი ახლა **პირდაპირ Supabase-დან** კითხულობს სექციებს/პროდუქტებს/hero-სლაიდებს — საწყისად სრულიად ცარიელი (0 პროდუქტი), გარდა 3 რეალური სექციისა (ყავა/შოკოლადი/ჩაი — ცარიელი, პროდუქტების გარეშე). პროდუქტები საიტზე გამოჩნდება მხოლოდ მას შემდეგ, რაც admin ხელით დაამატებს მათ admin პანელიდან.

## რა უნდა გაკეთდეს

### 1. Supabase-ში სექციების დამატება (admin-ის მიერ, არა კოდით)

Admin (მფლობელი) თავად დაამატებს 3 სექციას admin პანელის "სექციები" ტაბიდან:

| slug | name_ka | name_en |
|---|---|---|
| coffee | ყავა | Coffee |
| chocolate | შოკოლადი | Chocolate |
| tea | ჩაი | Tea |

**პროდუქტები არ ემატება** — სექციები დარჩება ცარიელი, სანამ admin თავად არ ატვირთავს რეალურ პროდუქტებს.

### 2. `script.js`-იდან ამოღება

- მთლიანად წაშალე `DATA` ობიექტი (ყველა placeholder პროდუქტი, `c`/`c2`/`short` ველებთან ერთად).
- მთლიანად წაშალე `HERO_SLIDES` ობიექტი (ფერადი გრადიენტები).
- ნებისმიერი ლოგიკა, რომელიც ამ ორ ობიექტზეა დამოკიდებული (კატეგორიის pill ღილაკები, პროდუქტის ბარათები, hero სლაიდერი) — გადაკეთდეს ისე, რომ Supabase-დან fetch-ილ მონაცემებს იყენებდეს.

### 3. Live fetch საჯარო საიტზე

- საჯარო საიტს (`index.html`/`script.js`) დაემატოს იგივე Supabase client-ის ინიციალიზაცია, რაც admin-ს აქვს (`@supabase/supabase-js` CDN + publishable key — **ეს key უკვე საჯაროა**, admin/config.js-ში ღიად ზის, უსაფრთხოა client-side-ზე, რადგან წვდომას RLS არეგულირებს).
- გვერდის ჩატვირთვისას (page load-ზე), fetch გაუშვი:
  - `sections` — მხოლოდ `is_visible=true`, დალაგებული `sort_order`-ით.
  - თითოეული სექციის `products` — მხოლოდ `is_visible=true`, დალაგებული `sort_order`-ით.
  - `hero_slides` — მხოლოდ `is_active=true`, დალაგებული `sort_order`-ით.
- კატეგორიის pill ღილაკები (ამჟამად fix-ირებული `.c-coffee`/`.c-chocolate`/`.c-tea` კლასებით) გახდეს **დინამიური** — აშენდეს fetch-ილი `sections`-იდან (name_ka/name_en/slug), არა hardcoded.

### 4. ცარიელი მდგომარეობების (empty state) მართვა

- **Hero სლაიდერი, როცა `hero_slides` ცარიელია:** slider ბლოკი საერთოდ არ გამოჩნდეს (დამალული იყოს), ნაცვლად ძველი ფერადი გრადიენტისა და "ფოტო მალე დაემატება" ტექსტისა.
- **პროდუქტების ბადრაგი, როცა სექციას 0 პროდუქტი აქვს:** არცერთი placeholder ბარათი არ დაირენდეროს — ან სრულიად ცარიელი ადგილი დარჩეს, ან მინიმალური ნეიტრალური ტექსტი (მაგ. "მალე დაემატება ახალი პროდუქტები") — **არავითარი ფერადი გრადიენტი ან ხელოვნური ბარათი**.
- როცა admin რეალურ პროდუქტს/სლაიდს დაამატებს admin პანელიდან, გვერდის ჩატვირთვისას ის ავტომატურად გამოჩნდება — დამატებითი კოდის ცვლილება აღარ დასჭირდება.

### 5. რაც ხელუხლებელი უნდა დარჩეს

- routing (`pushState`/`popstate`, `/კატეგორია/slug` ბმულები)
- SEO (`updateSEO` გამოძახებები)
- View Transitions API-ის ანიმაციები
- ენის გადართვა (KA/EN), თემის toggle, cookie-banner — ეს არცერთი ამ ცვლილებით არ იცვლება

## ტექნიკური რისკი (Claude Code-მ თავადვე აღნიშნა ადრე)

ეს ცვლილება routing/SEO/ანიმაციებთან ერთად მუშაობს — ფრთხილად იყავი, ნაბიჯ-ნაბიჯ გადაამოწმე, რომ არსებული ნავიგაცია (კატეგორიაზე დაჭერა, პროდუქტის გვერდზე გადასვლა, back/forward ღილაკები) არ დაზიანდეს.

## Out of scope

- admin პანელის კოდი (`admin/`) — არ იცვლება ამ დავალებაში.
- `404.html` — არ იცვლება.

## მისაღები კრიტერიუმები

- [ ] `DATA` და `HERO_SLIDES` აღარ არსებობს `script.js`-ში.
- [ ] საიტი ჩატვირთვისას Supabase-დან იღებს სექციებს/პროდუქტებს/hero-სლაიდებს.
- [ ] კატეგორიის pill ღილაკები (ყავა/შოკოლადი/ჩაი) დინამიურად შენდება `sections` ცხრილიდან — იგივე რაც admin-შიც ჩანს.
- [ ] 0 hero-სლაიდის დროს, slider არ ჩანს (არა ფერადი placeholder).
- [ ] 0 პროდუქტის მქონე სექციაში, არცერთი ხელოვნური ბარათი არ ჩანს.
- [ ] admin-ის მიერ ახლად დამატებული სექცია/პროდუქტი/hero ფოტო საიტზე რეალურად გამოჩნდება გვერდის განახლების შემდეგ.
- [ ] routing, SEO, ანიმაციები — ძველებურად მუშაობს.
