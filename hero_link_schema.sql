-- =========================================================
-- Doll's Coffee — hero_slides.link_url
-- ერთი დამატება: hero სლაიდს შეუძლია (არასავალდებულო) ლინკი ჰქონდეს,
-- რომელზეც დაკლიკება მიჰყავს ვიზიტორს. ცარიელი/NULL == ჩვეულებრივი,
-- დაუკლიკებადი სლაიდი, ისე როგორც აქამდე იყო.
-- =========================================================

alter table public.hero_slides add column if not exists link_url text;
