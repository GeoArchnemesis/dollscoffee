# BLOG_CONTACT_ABOUT_PRD — ბლოგი, კონტაქტი და ჩვენს შესახებ გვერდების admin-იზაცია

**სტატუსი:** დასამტკიცებელი → შესასრულებელი
**თარიღი:** 2026-09-24

## ფონი / ამჟამინდელი მდგომარეობა

`script.js`-ში ამჟამად:
- `POSTS` — 3 hardcoded placeholder ბლოგ-პოსტი, ველებით: `id`, `cat{ka,en}` (კატეგორიის ლეიბლი — სწორედ ეს ჩნდება ბლოგის ბარათზე, როგორც "ყავა"/"ჩაი"/"შოკოლადი" — არასწორია, ბლოგს კატეგორია არ სჭირდება), `c`/`c2` (ფერადი placeholder, რეალური ფოტო არასდროს ჰქონია), `date{ka,en}` (ცარიელი), `title{ka,en}`, `body{ka,en}` (ცარიელი მასივი).
- კონტაქტის გვერდის ტელეფონი/email/მისამართი **hardcoded** არის `index.html`-ში პირდაპირ (ტელეფონი: `+995 599 996 052`, email: `info@dollscoffee.com`, მისამართი: "საქართველო, თბილისი, ვასილ კოპცოვის 34ბ" / "34b Vasil Koptsovi St, Tbilisi, Georgia"). კონტაქტის ფორმა (სახელი/email/შეტყობინება) submit-ზე უბრალოდ `alert()`-ს აჩვენებს — **არაფერი არსად არ ინახება**.
- "ჩვენს შესახებ" გვერდს აქვს სათაური (`aboutTitle` — ამჟამად უბრალოდ "ჩვენს შესახებ"/"About Us") და ცარიელი lead-პარაგრაფი (`aboutLead`).
- admin პანელს ამჟამად აქვს: სლაიდერის ფოტოები, სექციები, პროდუქტები. **ბლოგის, კონტაქტისა და "ჩვენს შესახებ" მართვა საერთოდ არ არსებობს.**

## მიზანი

1. ბლოგის სრული მართვა admin-იდან (ბილინგვური, cover ფოტოთი, rich text ტექსტით).
2. კონტაქტის ინფორმაცია (ტელეფონი/email/მისამართი) გახდეს admin-იდან რედაქტირებადი.
3. საიტის კონტაქტის ფორმა რეალურად შეინახოს შეტყობინებები Supabase-ში, admin-მა დაინახოს ისინი პანელიდან.
4. "ჩვენს შესახებ" გვერდის სათაური/ტექსტი გახდეს admin-იდან რედაქტირებადი.
5. ყველაფერი ბილინგვური (KA/EN), არსებული `name_ka`/`name_en` კონვენციის მიხედვით.

## ტექნოლოგიური არჩევანი (კვლევის საფუძველზე)

- **Rich text editor: [Quill.js](https://quilljs.com/)** — CDN-ით ჩასართავი, build-ის გარეშე მუშაობს, მსუბუქია, აქვს საჭირო toolbar (bold/italic/underline/სათაურის ზომები/ფოტოს ჩასმა). ეს ერგება პროექტის "ვანილა JS, build-ის გარეშე" პრინციპს ისე, როგორც TipTap-ი ან სხვა React-ზე ორიენტირებული ალტერნატივები ვერ იქნებოდა.
- **HTML სანიტაცია: [DOMPurify](https://github.com/cure53/DOMPurify)** — Quill-იდან გამოსული HTML უნდა გაიწმინდოს XSS-რისკებისგან, როგორც შენახვამდე (admin-ში), ისე — რაც კრიტიკულია — **საჯარო საიტზე რენდერამდე**, რადგან ეს კონტენტი პირდაპირ HTML-ად ჩაისმება გვერდში.

## 1. ბლოგის მართვა

### Supabase სქემა — ახალი ცხრილი `blog_posts`

| ველი | ტიპი | აღწერა |
|---|---|---|
| id | uuid pk | |
| slug | text unique | URL-ისთვის (მაგ. `chveni-istoria`) — ავტომატურად გენერირდება `title_en`-იდან, ხელით რედაქტირებადი |
| title_ka / title_en | text not null | |
| cover_photo_url | text | ერთი cover ფოტო — ჩანს სიაშიც (thumbnail) და სტატიის თავშიც |
| body_ka / body_en | text | Quill-იდან გამოსული, **სანიტირებული** HTML |
| published_at | date | რეალური თარიღი (არა per-language ტექსტი, როგორც ძველ `POSTS`-ში იყო) |
| is_published | boolean default true | |
| created_at / updated_at | timestamptz | სტანდარტული ტრიგერი |

RLS: public SELECT მხოლოდ `is_published=true`-ზე; INSERT/UPDATE/DELETE მხოლოდ `is_admin()`-ით (იგივე პატერნი, რაც `sections`/`products`-ზეა).

### Admin UI — ახალი ტაბი "ბლოგი"

- სია: cover thumbnail, სათაური, გამოქვეყნების თარიღი, publish/unpublish toggle, რედაქტირება/წაშლა.
- "+ ახალი პოსტი": `title_ka`, `title_en` (slug ავტომატურად გენერირდება `title_en`-იდან, იგივე ლოგიკა რაც ADMINUPDATE-ში პროდუქტებზე დაინერგა), cover ფოტოს ატვირთვა (რეკომენდებული ზომა ~1200×630, კომპრესია იგივე ტექნიკით რაც hero/product ფოტოებზეა), `published_at` თარიღის picker, `is_published` toggle, და **ორი Quill.js რედაქტორი** (KA/EN — ჩანართებით ან ერთმანეთის ქვეშ) toolbar-ით: bold, italic, underline, სათაურის ზომები, ფოტოს ჩასმა ტექსტში.
- შენახვამდე, `body_ka`/`body_en` გაატარე DOMPurify-ში.

### საჯარო საიტის ცვლილება

- `POSTS` მთლიანად ამოღდეს კოდიდან.
- ბლოგის სია fetch-დეს Supabase-დან (`is_published=true`, დალაგებული `published_at`-ით კლებადობით).
- ბლოგის ბარათზე **აღარ** გამოჩნდეს კატეგორიის ლეიბლი ("ყავა"/"ჩაი"/"შოკოლადი") — ბლოგს სექციასთან კავშირი აღარ აქვს. მხოლოდ cover ფოტო + სათაური + თარიღი.
- სტატიის სრულ გვერდზე `body_ka`/`body_en` HTML **სანიტირებული სახით** (DOMPurify) ჩაისმას გვერდში.
- Routing: `/blog/slug` (ნაცვლად ამჟამინდელი `p1`/`p2`-ის).

## 2. კონტაქტის ინფორმაცია + კონტაქტის ფორმის შეტყობინებები

### Supabase სქემა — `contact_info` (ერთი მწკრივი, singleton)

| ველი | ტიპი |
|---|---|
| id | integer primary key (ყოველთვის `1`) |
| phone | text |
| email | text |
| address_ka / address_en | text |
| updated_at | timestamptz |

RLS: public SELECT; UPDATE მხოლოდ `is_admin()`-ით (INSERT/DELETE არ სჭირდება — მწკრივი წინასწარ ჩაიწერება schema-ს გაშვებისას, საწყისი მნიშვნელობებით: ტელეფონი `+995 599 996 052`, email `info@dollscoffee.com`, მისამართი "საქართველო, თბილისი, ვასილ კოპცოვის 34ბ" / "34b Vasil Koptsovi St, Tbilisi, Georgia" — ანუ ზუსტად ის, რაც ახლა უკვე საიტზეა, რომ ცვლილება ვიზუალურად უხილავი იყოს, სანამ admin თავად არაფერს არ შეცვლის).

### Supabase სქემა — ახალი ცხრილი `contact_messages`

| ველი | ტიპი |
|---|---|
| id | uuid pk |
| name | text not null |
| email | text not null |
| message | text not null |
| is_read | boolean default false |
| created_at | timestamptz |

RLS: **public INSERT მხოლოდ** (ანუ ვინმეს, ვინც ფორმას ავსებს, შეუძლია ახალი მწკრივის დამატება, მაგრამ **ვერავინ ვერ წაიკითხავს/ვერ შეცვლის/ვერ წაშლის** გარდა admin-ისა — ეს კრიტიკულია, სხვა შემთხვევაში ნებისმიერს შეეძლებოდა სხვისი შეტყობინებების ნახვა). SELECT/UPDATE(`is_read`)/DELETE მხოლოდ `is_admin()`-ით.

### Admin UI

- ტაბი/სექცია "კონტაქტი" (შეიძლება გაერთიანდეს "გვერდები" ტაბში, იხ. ქვემოთ): ფორმა `phone`/`email`/`address_ka`/`address_en` რედაქტირებისთვის, "შენახვა" ღილაკით.
- ცალკე ტაბი "შეტყობინებები": სია — გამომგზავნის სახელი/email/ტექსტი/თარიღი, "წაკითხულად მონიშვნა", წაშლა.

### საჯარო საიტის ცვლილება

- Contact გვერდის ტელეფონი/email/მისამართი `contact_info`-დან fetch-დეს (ამჟამად hardcoded HTML-ია `index.html`-ში).
- კონტაქტის ფორმის `onsubmit` შეიცვალოს — `alert()`-ის მაგივრად, რეალურად ჩაწეროს `contact_messages`-ში (public anon key-ით, INSERT-only RLS-ის წყალობით უსაფრთხოა), მერე აჩვენოს წარმატების შეტყობინება (არსებული `T[lang].sent` ტექსტი შეიძლება დარჩეს).

## 3. "ჩვენს შესახებ" გვერდი

### Supabase სქემა — `about_page` (ერთი მწკრივი, singleton)

| ველი | ტიპი |
|---|---|
| id | integer primary key (ყოველთვის `1`) |
| title_ka / title_en | text |
| body_ka / body_en | text (ჩვეულებრივი ტექსტი, არა Quill — თუ მომავალში rich text დაგჭირდება აქაც, ცალკე მოთხოვნით დაემატება) |
| updated_at | timestamptz |

RLS: public SELECT; UPDATE მხოლოდ `is_admin()`-ით.

### Admin UI

ტაბი/სექცია "ჩვენს შესახებ" (ან იგივე "გვერდები" ტაბის ნაწილი): `title_ka`/`title_en`, `body_ka`/`body_en` (textarea), "შენახვა".

### საჯარო საიტის ცვლილება

About გვერდის სათაური/lead ტექსტი `about_page`-დან fetch-დეს, ნაცვლად ამჟამინდელი ცარიელი/ფიქსირებული `T[lang].aboutTitle`/`aboutLead`-ისა.

## Admin პანელის ნავიგაცია — შემოთავაზებული ტაბების საბოლოო სია

`სლაიდერის ფოტოები | სექციები | პროდუქტები | ბლოგი | გვერდები (კონტაქტი + ჩვენს შესახებ) | შეტყობინებები`

("გვერდები" აერთიანებს კონტაქტისა და "ჩვენს შესახებ" მართვას ერთ ტაბში, ორი სექციად — რომ ტაბების რაოდენობა არ გაიზარდოს გადამეტებულად. თუ Claude Code სხვაგვარად სჯობს ჩათვლის, შემოგვთავაზოს ალტერნატივა.)

## Out of scope (ამ ეტაპზე)

- ფუტერის სოციალური ბმულები (Facebook/Instagram და ა.შ.) — **კოდში რჩება hardcoded**, admin ხელით შეცვლის საჭიროებისამებრ. ეს არ შედის ამ დავალებაში.
- Privacy/Terms გვერდები — უცვლელი რჩება.
- Blog-ის body-ისთვის tag/category სისტემა — არ არის მოთხოვნილი, არ ემატება.

## მისაღები კრიტერიუმები

- [ ] `POSTS` აღარ არსებობს კოდში; ბლოგი Supabase-დან იტვირთება.
- [ ] ბლოგის ბარათებზე კატეგორიის ლეიბლი (ყავა/ჩაი/შოკოლადი) აღარ ჩანს.
- [ ] admin-იდან ბლოგის პოსტის დამატება/რედაქტირება/წაშლა მუშაობს, ორივე ენაზე, Quill.js რედაქტორით.
- [ ] კონტაქტის ინფორმაცია (ტელეფონი/email/მისამართი) admin-იდან რედაქტირებადია და საიტზე აისახება.
- [ ] კონტაქტის ფორმის შევსება რეალურად ინახავს მონაცემს, admin ხედავს "შეტყობინებები" ტაბში.
- [ ] "ჩვენს შესახებ" გვერდის სათაური/ტექსტი admin-იდან რედაქტირებადია.
- [ ] ყველა ახალი ველი მუშაობს ორივე ენაზე (KA/EN).
- [ ] Blog body HTML სანიტირებულია (DOMPurify) როგორც შენახვისას, ისე რენდერისას.
